import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test – full widget tests require provider mocks.
    expect(1 + 1, equals(2));
  });
}
